name: HAaaS Legacy Placeholder Notice

on:
  workflow_dispatch:

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: echo "Primary validation workflow is status.yml. This manual placeholder remains for historical continuity."
