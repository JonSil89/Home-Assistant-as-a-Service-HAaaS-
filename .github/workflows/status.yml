name: CI Pipeline

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Verify Environment
        run: |
          echo "Environment is stable."
          echo "CI/CD pipeline check successful.""Environment is stable and dependencies are cached."
