# AI layer dependencies
# Pinned to stable minor versions; update via: pip install -U -r ai/requirements.txt
#
# Core web framework
fastapi==0.115.6

# ASGI server
uvicorn[standard]==0.32.1

# Data validation (FastAPI peer dep — explicit pin for reproducibility)
pydantic==2.10.3

# ---------------------------------------------------------------------------
# Optional — uncomment when enabling real integrations:
# ---------------------------------------------------------------------------
# httpx==0.27.2          # Async HTTP client for HA REST API calls
# redis==5.2.1           # Redis backend for STM (RedisSTM)
# python-dotenv==1.0.1   # Load .env files in local dev
